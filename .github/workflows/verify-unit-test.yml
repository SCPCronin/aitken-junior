name: MVN Verify
run-name: ${{ github.actor }} is running Verification
on:
  push:
    branches: [ $default-branch ] # Runs on pushes to default branch
  pull_request:
    branches: [ $default-branch ] # Runs on any PRs to default branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 23
        uses: actions/setup-java@v4
        with:
          java-version: '23'
          distribution: 'temurin'
          cache: maven
      - name: Run MVN Verify
        run: mvn verify
